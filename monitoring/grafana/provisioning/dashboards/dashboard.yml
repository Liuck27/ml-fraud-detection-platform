# Grafana dashboard provisioning config. Full implementation in Phase 9.
apiVersion: 1
providers:
  - name: fraud-detection
    folder: Fraud Detection
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
