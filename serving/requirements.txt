# Serving service — FastAPI, Uvicorn, Pydantic, MLflow.
# Used in: serving/.venv (local dev) and serving/Dockerfile (Phase 4+).

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.3
pydantic-settings==2.1.0
mlflow==2.9.2
prometheus-fastapi-instrumentator==6.1.0
httpx==0.26.0
# ML runtime deps — needed to load and run registered models
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
torch>=2.1        # CPU build sufficient; install torch+cpu wheel if disk space matters
shap>=0.44
# Test deps
pytest==7.4.4
