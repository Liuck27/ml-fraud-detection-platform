# Serving service — FastAPI, Uvicorn, Pydantic, MLflow.
# Used in: serving/.venv (local dev) and serving/Dockerfile (Phase 4+).

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.3
pydantic-settings==2.1.0
mlflow==2.9.2
prometheus-fastapi-instrumentator==6.1.0
httpx==0.26.0
# ML runtime deps — pinned to match training/requirements.txt exactly.
# Version mismatches between training and serving cause model loading failures
# (scikit-learn mixin API changes, XGBoost binary format changes, etc.).
numpy==1.26.3
pandas==2.1.4
scikit-learn==1.3.2
xgboost==2.0.3
torch==2.1.2
shap>=0.44
# Test deps
pytest==7.4.4
