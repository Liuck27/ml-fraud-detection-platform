# Training service — scikit-learn, XGBoost, PyTorch, pandas, MLflow.
# Used in: training/.venv (local dev) and training/Dockerfile (Phase 4+).
# Note: torch installs CPU-only build on Windows by default — correct for local dev.

scikit-learn==1.3.2
xgboost==2.0.3
torch==2.1.2
pandas==2.1.4
numpy==1.26.3
mlflow==2.9.2
matplotlib==3.8.2
seaborn==0.13.0
imbalanced-learn==0.11.0
jupyter==1.0.0
ipykernel==6.28.0
pytest==7.4.4
mypy==1.7.1
