# Root dev tools — installed into .venv at project root.
# NOT used in any Docker image. Provides linting, formatting, and CI tooling.

ruff==0.1.9
black==23.12.1
mypy==1.7.1
pytest==7.4.4
pytest-cov==4.1.0
pip-audit==2.6.1
